@page
@model CodeRacer.Pages.Submissions.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Submission</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Submission.Owner)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Submission.Owner.Id)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Submission.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
